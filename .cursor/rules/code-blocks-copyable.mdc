---
description: 需用户改代码时，用带语言与语法高亮的代码块并便于复制
alwaysApply: true
---

# 用户编辑代码时的呈现方式

凡需要**用户**在本机/编辑器中**自己修改、粘贴或补全的代码、配置、JSON、命令等**：

- 必须使用**独立的 Markdown 代码块**（围栏代码块）给出，使界面里能显示**语法高亮**、便于区分关键字与字符串等。
- 代码块**第一行**必须带**语言/格式标识**，方便复制后仍保留高亮，例如：  
  ` ```python `、` ```html `、` ```json `、` ```env `、` ```bash `、` ```cmd ` 等。纯文本、无高亮要求时用 ` ```text ` 亦可。
- 须保证内容**可一次性全选、复制、粘贴**；除必要说明外，**不要**在代码块前一句里夹带半截代码而不放进框内。
- 需改动的文件、路径可在代码块**上方**用短句标出，但**可复制的正文仍在代码块内**。

**避免**：只用行内反引号包一大段、或只描述「在某某函数里加几行」而不贴出**完整、可复制**的片段。若改动较多，可分段用多个带语言标签的代码块，或给出一整块完整文件/片段。

目的：你复制到编辑器时，有颜色区分、少漏字、少混进说明文字。
