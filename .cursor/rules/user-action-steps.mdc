---
description: 用户可执行操作须写出完整命令，勿仅写操作名称
alwaysApply: true
---

# 用户动手的步骤

凡需要用户在本机、服务器上亲自执行的操作，须按**可复制的步骤**说明：

- 每一步写明**环境**（如 cmd、PowerShell、bash）；
- 写明**工作目录**或 `cd` 的完整路径；
- 给出**一条可直接粘贴运行的完整命令**（或等价的明确操作路径）；
- **不要**只写「然后重启服务」「去提交一下」等笼统名称，而不给出对应的具体指令。

---

需要用户动手的步骤，每步用可复制指令写出（含环境、目录、完整命令），不要只写操作名称。
